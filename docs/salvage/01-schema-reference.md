# Phase 0 Salvage — Schema Reference

> AUTO-GENERATED by `scripts/salvage-schema.sh` from `prisma/schema.prisma`.
> Don't edit by hand. Re-run to regenerate.

This file captures the **domain decomposition** of the ERP repo at the
point it was archived. The Phase 1 kernel will not import this schema
directly — but each model here represents one or more design decisions
about the business domain (Egyptian pharma ERP), and those decisions
are worth referencing when designing Phase 1 modules.

## How to use this in Phase 1

- Use the model list as a **domain map**. Each model = one bounded
  concept. Decide which modules in Phase 1 own which models.
- Pull field shapes when scaffolding equivalent Phase 1 tables. The
  field types and indexes encode real-world experience.
- The `tenantId` column on every model (added in Phase 0 Track B2.1)
  is the multi-tenancy contract — preserve it.
- Enums (separate doc: `02-domain-vocabulary.md`) capture business
  states; copy them verbatim into Phase 1 unless you have a reason to
  rename.

## Model index

- [`AIConfig`](#aiconfig) → table `ai_configs`
- [`AILog`](#ailog) → table `ai_logs`
- [`Account`](#account) → table `accounts`
- [`Activity`](#activity) → table `activities`
- [`ApiToken`](#apitoken) → table `api_tokens`
- [`Application`](#application) → table `applications`
- [`ApprovalLog`](#approvallog) → table `approval_logs`
- [`AssetMaintenance`](#assetmaintenance) → table `asset_maintenances`
- [`Asset`](#asset) → table `assets`
- [`Attendance`](#attendance) → table `attendances`
- [`AuditLog`](#auditlog) → table `audit_logs`
- [`BOMItem`](#bomitem) → table `bom_items`
- [`BatchRelease`](#batchrelease) → table `batch_releases`
- [`BatchTraceability`](#batchtraceability) → table `batch_traceabilities`
- [`BillItem`](#billitem) → table `bill_items`
- [`BillOfMaterials`](#billofmaterials) → table `bill_of_materials`
- [`Bill`](#bill) → table `bills`
- [`BudgetLine`](#budgetline) → table `budget_lines`
- [`Budget`](#budget) → table `budgets`
- [`BusinessUnitMember`](#businessunitmember) → table `business_unit_members`
- [`BusinessUnitProduct`](#businessunitproduct) → table `business_unit_products`
- [`BusinessUnitTerritory`](#businessunitterritory) → table `business_unit_territories`
- [`BusinessUnit`](#businessunit) → table `business_units`
- [`CAPAAction`](#capaaction) → table `capa_actions`
- [`CAPA`](#capa) → table `capas`
- [`Campaign`](#campaign) → table `campaigns`
- [`Candidate`](#candidate) → table `candidates`
- [`ChangeControl`](#changecontrol) → table `change_controls`
- [`ChartOfAccount`](#chartofaccount) → table `chart_of_accounts`
- [`Cheque`](#cheque) → table `cheques`
- [`CleaningValidation`](#cleaningvalidation) → table `cleaning_validations`
- [`Contact`](#contact) → table `contacts`
- [`Contract`](#contract) → table `contracts`
- [`CostCenter`](#costcenter) → table `cost_centers`
- [`CustomFieldValue`](#customfieldvalue) → table `custom_field_values`
- [`CustomField`](#customfield) → table `custom_fields`
- [`DealActivity`](#dealactivity) → table `deal_activities`
- [`Deal`](#deal) → table `deals`
- [`DeliveryNote`](#deliverynote) → table `delivery_notes`
- [`Department`](#department) → table `departments`
- [`Deviation`](#deviation) → table `deviations`
- [`Doctor`](#doctor) → table `doctors`
- [`Document`](#document) → table `documents`
- [`Employee`](#employee) → table `employees`
- [`EnvironmentalMonitoring`](#environmentalmonitoring) → table `environmental_monitorings`
- [`EquipmentRecord`](#equipmentrecord) → table `equipment_records`
- [`Expense`](#expense) → table `expenses`
- [`FieldVisit`](#fieldvisit) → table `field_visits`
- [`FiscalPeriod`](#fiscalperiod) → table `fiscal_periods`
- [`GPSLocation`](#gpslocation) → table `gps_locations`
- [`GoodsReceipt`](#goodsreceipt) → table `goods_receipts`
- [`HVACReading`](#hvacreading) → table `hvac_readings`
- [`Interview`](#interview) → table `interviews`
- [`InvoiceItem`](#invoiceitem) → table `invoice_items`
- [`Invoice`](#invoice) → table `invoices`
- [`Job`](#job) → table `jobs`
- [`JournalEntryLine`](#journalentryline) → table `journal_entry_lines`
- [`JournalEntry`](#journalentry) → table `journal_entries`
- [`KPI`](#kpi) → table `kpis`
- [`KnowledgeArticle`](#knowledgearticle) → table `knowledge_articles`
- [`Lead`](#lead) → table `leads`
- [`LeaveRequest`](#leaverequest) → table `leave_requests`
- [`LoyaltyMember`](#loyaltymember) → table `loyalty_members`
- [`LoyaltyProgram`](#loyaltyprogram) → table `loyalty_programs`
- [`LoyaltyTransaction`](#loyaltytransaction) → table `loyalty_transactions`
- [`ManufacturingBatchRecord`](#manufacturingbatchrecord) → table `manufacturing_batch_records`
- [`MarketRequest`](#marketrequest) → table `market_requests`
- [`Message`](#message) → table `messages`
- [`Notification`](#notification) → table `notifications`
- [`OOSInvestigation`](#oosinvestigation) → table `oos_investigations`
- [`OfferLetter`](#offerletter) → table `offer_letters`
- [`OnboardingChecklist`](#onboardingchecklist) → table `onboarding_checklists`
- [`OnboardingTask`](#onboardingtask) → table `onboarding_tasks`
- [`Opportunity`](#opportunity) → table `opportunities`
- [`Payment`](#payment) → table `payments`
- [`Payroll`](#payroll) → table `payrolls`
- [`Position`](#position) → table `positions`
- [`Product`](#product) → table `products`
- [`ProjectTask`](#projecttask) → table `project_tasks`
- [`Project`](#project) → table `projects`
- [`PurchaseOrderItem`](#purchaseorderitem) → table `purchase_order_items`
- [`PurchaseOrder`](#purchaseorder) → table `purchase_orders`
- [`QAuditFinding`](#qauditfinding) → table `q_audit_findings`
- [`QAudit`](#qaudit) → table `q_audits`
- [`QComplaint`](#qcomplaint) → table `q_complaints`
- [`QDocumentVersion`](#qdocumentversion) → table `q_document_versions`
- [`QDocument`](#qdocument) → table `q_documents`
- [`RFQ`](#rfq) → table `rfqs`
- [`Recall`](#recall) → table `recalls`
- [`RiskAssessment`](#riskassessment) → table `risk_assessments`
- [`SPCChart`](#spcchart) → table `spc_charts`
- [`SPCDataPoint`](#spcdatapoint) → table `spc_data_points`
- [`SalesOrderItem`](#salesorderitem) → table `sales_order_items`
- [`SalesOrder`](#salesorder) → table `sales_orders`
- [`Shipment`](#shipment) → table `shipments`
- [`StabilityStudy`](#stabilitystudy) → table `stability_studies`
- [`StabilityTimepoint`](#stabilitytimepoint) → table `stability_timepoints`
- [`StandaloneTask`](#standalonetask) → table `standalone_tasks`
- [`StockMovement`](#stockmovement) → table `stock_movements`
- [`SupplierQuality`](#supplierquality) → table `supplier_qualities`
- [`Supplier`](#supplier) → table `suppliers`
- [`SystemSetting`](#systemsetting) → table `system_settings`
- [`TaxConfiguration`](#taxconfiguration) → table `tax_configurations`
- [`Territory`](#territory) → table `territories`
- [`ThreeWayMatch`](#threewaymatch) → table `three_way_matches`
- [`Ticket`](#ticket) → table `tickets`
- [`TimeEntry`](#timeentry) → table `time_entries`
- [`TrainingCourse`](#trainingcourse) → table `training_courses`
- [`TrainingEnrollment`](#trainingenrollment) → table `training_enrollments`
- [`TrainingRecord`](#trainingrecord) → table `training_records`
- [`User`](#user) → table `users`
- [`VendorScore`](#vendorscore) → table `vendor_scores`
- [`Visit`](#visit) → table `visits`
- [`WarehouseZone`](#warehousezone) → table `warehouse_zones`
- [`Warehouse`](#warehouse) → table `warehouses`
- [`WaterSystemReading`](#watersystemreading) → table `water_system_readings`
- [`WeeklyPlan`](#weeklyplan) → table `weekly_plans`
- [`WorkOrder`](#workorder) → table `work_orders`
- [`WorkflowLog`](#workflowlog) → table `workflow_logs`
- [`Workflow`](#workflow) → table `workflows`

## Models

### User

- **Table:** `User`
- **Definition:**

```prisma
model User {
  id           String   @id @default(cuid())
  tenantId     String
  name         String
  email        String
  passwordHash String
  role         String   @default("EMPLOYEE") // ADMIN, MANAGER, EMPLOYEE
  avatar       String?
  department   String?
  territory    String?
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  apiTokens       ApiToken[]
  auditLogs       AuditLog[]
  notifications   Notification[]
  workflows       Workflow[]
  journalEntries  JournalEntry[]
  purchaseOrders  PurchaseOrder[]
  stockMovements  StockMovement[]
  projects        Project[]       @relation("ProjectManager")
  assignedTasks   ProjectTask[]   @relation("TaskAssignee")
  timeEntries     TimeEntry[]
  managedDepts    Department[]    @relation("DeptManager")
  approvedLeaves  LeaveRequest[]  @relation("LeaveApprover")
  assignedLeads   Lead[]
  assignedOpps    Opportunity[]
  activities      Activity[]
  ownedAccounts   Account[]
  ownedContacts   Contact[]
  ownedCampaigns  Campaign[]
  assignedTickets Ticket[]
  articles        KnowledgeArticle[]
  fieldVisits     FieldVisit[]
  gpsLocations    GPSLocation[]
  territories     Territory[]
  documents       Document[]
  managedWH       Warehouse[]
  onboardTasks    OnboardingTask[] @relation("OnboardAssigner")
  hiringJobs      Job[]
  managedBUs      BusinessUnit[]         @relation("BUManager")
  buMemberships   BusinessUnitMember[]
  approvalActions ApprovalLog[]          @relation("ApprovalPerformer")

  // Pharma CRM relations
  assignedDoctors    Doctor[]        @relation("DoctorRep")
  repVisits          Visit[]         @relation("VisitRep")
  repWeeklyPlans     WeeklyPlan[]    @relation("PlanRep")
  approvedPlans      WeeklyPlan[]    @relation("PlanApprover")
  marketRequests     MarketRequest[] @relation("RequestedBy")
  approvedRequests   MarketRequest[] @relation("RequestApprover")
  submittedExpenses  Expense[]       @relation("ExpenseSubmitter")
  approvedExpenses   Expense[]       @relation("ExpenseApprover")
  kpis               KPI[]
  assignedStandaloneTasks  StandaloneTask[]  @relation("StandaloneTaskAssignee")
  createdStandaloneTasks   StandaloneTask[]  @relation("StandaloneTaskCreator")
  sentMessages       Message[]       @relation("MessageSender")
  receivedMessages   Message[]       @relation("MessageRecipient")
  ownedDeals         Deal[]          @relation("DealOwner")

  @@unique([tenantId, email])
  @@index([tenantId])
  @@map("users")

}
```

### ApiToken

- **Table:** `ApiToken`
- **Definition:**

```prisma
model ApiToken {
  id          String    @id @default(cuid())
  tenantId    String
  userId      String
  user        User      @relation(fields: [userId], references: [id])
  name        String
  token       String    @unique
  permissions String    @default("[]") // JSON array
  lastUsed    DateTime?
  expiresAt   DateTime?
  isActive    Boolean   @default(true)
  createdAt   DateTime  @default(now())

  @@index([tenantId])
  @@map("api_tokens")

}
```

### AuditLog

- **Table:** `AuditLog`
- **Definition:**

```prisma
model AuditLog {
  id        String   @id @default(cuid())
  tenantId  String
  userId    String?
  user      User?    @relation(fields: [userId], references: [id])
  action    String
  entity    String
  entityId  String
  oldValues String?
  newValues String?
  changes   String?  // JSON diff of old/new values
  ipAddress String?
  createdAt DateTime @default(now())

  @@index([tenantId])
  @@index([entity, entityId])
  @@index([userId])
  @@map("audit_logs")

}
```

### Notification

- **Table:** `Notification`
- **Definition:**

```prisma
model Notification {
  id        String   @id @default(cuid())
  tenantId  String
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  title     String
  message   String
  type      String   @default("INFO") // INFO, WARNING, SUCCESS, ERROR
  isRead    Boolean  @default(false)
  link      String?
  module    String?  // CRM, ERP, HR, ATS, etc.
  createdAt DateTime @default(now())

  @@index([tenantId])
  @@index([userId, isRead])
  @@map("notifications")

}
```

### CustomField

- **Table:** `CustomField`
- **Definition:**

```prisma
model CustomField {
  id         String  @id @default(cuid())
  tenantId   String
  module     String
  fieldName  String
  fieldType  String  // TEXT, NUMBER, DATE, SELECT, BOOLEAN
  options    String? // JSON for SELECT options
  isRequired Boolean @default(false)
  sortOrder  Int     @default(0)

  values CustomFieldValue[]

  @@index([tenantId])
  @@map("custom_fields")

}
```

### CustomFieldValue

- **Table:** `CustomFieldValue`
- **Definition:**

```prisma
model CustomFieldValue {
  id            String      @id @default(cuid())
  tenantId      String
  customFieldId String
  customField   CustomField @relation(fields: [customFieldId], references: [id])
  entityId      String
  entityType    String
  value         String

  @@index([tenantId])
  @@index([entityType, entityId])
  @@map("custom_field_values")

}
```

### SystemSetting

- **Table:** `SystemSetting`
- **Definition:**

```prisma
model SystemSetting {
  id       String @id @default(cuid())
  tenantId String
  key      String @unique
  value    String
  category String @default("general")

  @@index([tenantId])
  @@map("system_settings")

}
```

### Workflow

- **Table:** `Workflow`
- **Definition:**

```prisma
model Workflow {
  id            String    @id @default(cuid())
  tenantId      String
  name          String
  module        String
  triggerType   String
  triggerConfig String    @default("{}") // JSON
  conditions    String    @default("{}") // JSON
  actions       String    @default("{}") // JSON
  isActive      Boolean   @default(true)
  createdById   String
  createdBy     User      @relation(fields: [createdById], references: [id])
  runCount      Int       @default(0)
  lastRunAt     DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  logs WorkflowLog[]

  @@index([tenantId])
  @@map("workflows")

}
```

### WorkflowLog

- **Table:** `WorkflowLog`
- **Definition:**

```prisma
model WorkflowLog {
  id         String   @id @default(cuid())
  tenantId   String
  workflowId String
  workflow   Workflow  @relation(fields: [workflowId], references: [id])
  status     String   // SUCCESS, FAILURE
  input      String?
  output     String?
  executedAt DateTime @default(now())

  @@index([tenantId])
  @@map("workflow_logs")

}
```

### AIConfig

- **Table:** `AIConfig`
- **Definition:**

```prisma
model AIConfig {
  id        String   @id @default(cuid())
  tenantId  String
  provider  String   // openai, anthropic, custom
  apiKey    String
  model     String
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  logs AILog[]

  @@index([tenantId])
  @@map("ai_configs")

}
```

### AILog

- **Table:** `AILog`
- **Definition:**

```prisma
model AILog {
  id         String   @id @default(cuid())
  tenantId   String
  configId   String?
  config     AIConfig? @relation(fields: [configId], references: [id])
  feature    String
  prompt     String
  response   String?
  tokensUsed Int?
  cost       Float?
  createdAt  DateTime @default(now())

  @@index([tenantId])
  @@map("ai_logs")

}
```

### ChartOfAccount

- **Table:** `ChartOfAccount`
- **Definition:**

```prisma
model ChartOfAccount {
  id        String   @id @default(cuid())
  tenantId  String
  code      String   @unique
  name      String
  type      String   // ASSET, LIABILITY, EQUITY, REVENUE, EXPENSE
  balance   Float    @default(0)
  parentId  String?
  parent    ChartOfAccount?  @relation("AccountHierarchy", fields: [parentId], references: [id])
  children  ChartOfAccount[] @relation("AccountHierarchy")
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  journalLines JournalEntryLine[]

  @@index([tenantId])
  @@map("chart_of_accounts")

}
```

### JournalEntry

- **Table:** `JournalEntry`
- **Definition:**

```prisma
model JournalEntry {
  id          String   @id @default(cuid())
  tenantId    String
  entryNumber String   @unique
  date        DateTime
  reference   String?
  description String
  status      String   @default("DRAFT") // DRAFT, POSTED, VOID
  createdById String
  createdBy   User     @relation(fields: [createdById], references: [id])
  createdAt   DateTime @default(now())

  lines JournalEntryLine[]

  @@index([tenantId])
  @@map("journal_entries")

}
```

### JournalEntryLine

- **Table:** `JournalEntryLine`
- **Definition:**

```prisma
model JournalEntryLine {
  id             String       @id @default(cuid())
  tenantId       String
  journalEntryId String
  journalEntry   JournalEntry @relation(fields: [journalEntryId], references: [id])
  accountId      String
  account        ChartOfAccount @relation(fields: [accountId], references: [id])
  debit          Float        @default(0)
  credit         Float        @default(0)
  description    String?

  @@index([tenantId])
  @@map("journal_entry_lines")

}
```

### Invoice

- **Table:** `Invoice`
- **Definition:**

```prisma
model Invoice {
  id            String   @id @default(cuid())
  tenantId      String
  invoiceNumber String   @unique
  customerId    String
  customer      Account  @relation(fields: [customerId], references: [id])
  date          DateTime
  dueDate       DateTime
  status        String   @default("DRAFT") // DRAFT, SENT, PAID, OVERDUE, VOID
  subtotal      Float
  tax           Float    @default(0)
  total         Float
  notes         String?
  createdAt     DateTime @default(now())

  items    InvoiceItem[]
  payments Payment[]     @relation("InvoicePayments")

  @@index([tenantId])
  @@map("invoices")

}
```

### InvoiceItem

- **Table:** `InvoiceItem`
- **Definition:**

```prisma
model InvoiceItem {
  id          String  @id @default(cuid())
  tenantId    String
  invoiceId   String
  invoice     Invoice @relation(fields: [invoiceId], references: [id])
  description String
  quantity    Float
  unitPrice   Float
  tax         Float   @default(0)
  total       Float

  @@index([tenantId])
  @@map("invoice_items")

}
```

### Bill

- **Table:** `Bill`
- **Definition:**

```prisma
model Bill {
  id         String   @id @default(cuid())
  tenantId   String
  billNumber String   @unique
  supplierId String
  supplier   Supplier @relation(fields: [supplierId], references: [id])
  date       DateTime
  dueDate    DateTime
  status     String   @default("DRAFT") // DRAFT, RECEIVED, PAID, OVERDUE, VOID
  subtotal   Float
  tax        Float    @default(0)
  total      Float
  notes      String?
  createdAt  DateTime @default(now())

  items    BillItem[]
  payments Payment[]  @relation("BillPayments")

  @@index([tenantId])
  @@map("bills")

}
```

### BillItem

- **Table:** `BillItem`
- **Definition:**

```prisma
model BillItem {
  id          String @id @default(cuid())
  tenantId    String
  billId      String
  bill        Bill   @relation(fields: [billId], references: [id])
  description String
  quantity    Float
  unitPrice   Float
  total       Float

  @@index([tenantId])
  @@map("bill_items")

}
```

### Payment

- **Table:** `Payment`
- **Definition:**

```prisma
model Payment {
  id        String   @id @default(cuid())
  tenantId  String
  type      String   // INCOMING, OUTGOING
  amount    Float
  date      DateTime
  method    String?  // CASH, BANK_TRANSFER, CHECK, CREDIT_CARD
  reference String?
  invoiceId String?
  invoice   Invoice? @relation("InvoicePayments", fields: [invoiceId], references: [id])
  billId    String?
  bill      Bill?    @relation("BillPayments", fields: [billId], references: [id])
  createdAt DateTime @default(now())

  @@index([tenantId])
  @@map("payments")

}
```

### Supplier

- **Table:** `Supplier`
- **Definition:**

```prisma
model Supplier {
  id           String   @id @default(cuid())
  tenantId     String
  name         String
  email        String?
  phone        String?
  address      String?
  city         String?
  country      String?
  status       String   @default("ACTIVE") // ACTIVE, INACTIVE
  rating       Float?
  paymentTerms String?
  createdAt    DateTime @default(now())

  bills          Bill[]
  purchaseOrders PurchaseOrder[]
  contracts      Contract[]

  @@index([tenantId])
  @@map("suppliers")

}
```

### PurchaseOrder

- **Table:** `PurchaseOrder`
- **Definition:**

```prisma
model PurchaseOrder {
  id           String   @id @default(cuid())
  tenantId     String
  poNumber     String   @unique
  supplierId   String
  supplier     Supplier @relation(fields: [supplierId], references: [id])
  date         DateTime
  expectedDate DateTime?
  status       String   @default("DRAFT") // DRAFT, APPROVED, SENT, RECEIVED, CANCELLED
  total        Float
  notes        String?
  createdById  String
  createdBy    User     @relation(fields: [createdById], references: [id])
  createdAt    DateTime @default(now())

  items PurchaseOrderItem[]

  @@index([tenantId])
  @@map("purchase_orders")

}
```

### PurchaseOrderItem

- **Table:** `PurchaseOrderItem`
- **Definition:**

```prisma
model PurchaseOrderItem {
  id              String        @id @default(cuid())
  tenantId        String
  purchaseOrderId String
  purchaseOrder   PurchaseOrder @relation(fields: [purchaseOrderId], references: [id])
  productId       String?
  product         Product?      @relation(fields: [productId], references: [id])
  description     String
  quantity        Float
  unitPrice       Float
  total           Float

  @@index([tenantId])
  @@map("purchase_order_items")

}
```

### Contract

- **Table:** `Contract`
- **Definition:**

```prisma
model Contract {
  id         String    @id @default(cuid())
  tenantId   String
  title      String
  supplierId String
  supplier   Supplier  @relation(fields: [supplierId], references: [id])
  type       String?
  startDate  DateTime
  endDate    DateTime?
  value      Float
  status     String    @default("DRAFT") // DRAFT, ACTIVE, EXPIRED, TERMINATED
  terms      String?
  createdAt  DateTime  @default(now())

  @@index([tenantId])
  @@map("contracts")

}
```

### Product

- **Table:** `Product`
- **Definition:**

```prisma
model Product {
  id           String   @id @default(cuid())
  tenantId     String
  sku          String   @unique
  name         String
  description  String?
  category     String?
  unitPrice    Float
  costPrice    Float
  quantity     Int      @default(0)
  reorderLevel Int      @default(10)
  unit         String   @default("pcs")
  status       String   @default("ACTIVE") // ACTIVE, DISCONTINUED
  createdAt    DateTime @default(now())

  stockMovements  StockMovement[]
  poItems         PurchaseOrderItem[]
  salesOrderItems SalesOrderItem[]
  boms            BillOfMaterials[]
  bomMaterials    BOMItem[]
  businessUnits   BusinessUnitProduct[]
  batchReleases   BatchRelease[]

  @@index([tenantId])
  @@map("products")

}
```

### Warehouse

- **Table:** `Warehouse`
- **Definition:**

```prisma
model Warehouse {
  id        String   @id @default(cuid())
  tenantId  String
  name      String
  location  String?
  capacity  Int?
  managerId String?
  manager   User?    @relation(fields: [managerId], references: [id])
  createdAt DateTime @default(now())

  stockMovements StockMovement[]

  @@index([tenantId])
  @@map("warehouses")

}
```

### StockMovement

- **Table:** `StockMovement`
- **Definition:**

```prisma
model StockMovement {
  id          String    @id @default(cuid())
  tenantId    String
  productId   String
  product     Product   @relation(fields: [productId], references: [id])
  warehouseId String?
  warehouse   Warehouse? @relation(fields: [warehouseId], references: [id])
  type        String    // IN, OUT, TRANSFER, ADJUSTMENT
  quantity    Int
  date        DateTime
  reference   String?
  notes       String?
  createdById String
  createdBy   User      @relation(fields: [createdById], references: [id])

  @@index([tenantId])
  @@map("stock_movements")

}
```

### SalesOrder

- **Table:** `SalesOrder`
- **Definition:**

```prisma
model SalesOrder {
  id              String   @id @default(cuid())
  tenantId        String
  orderNumber     String   @unique
  customerId      String
  customer        Account  @relation(fields: [customerId], references: [id])
  date            DateTime
  status          String   @default("PENDING") // PENDING, CONFIRMED, SHIPPED, DELIVERED, CANCELLED
  total           Float
  shippingAddress String?
  notes           String?
  createdAt       DateTime @default(now())

  items SalesOrderItem[]

  @@index([tenantId])
  @@map("sales_orders")

}
```

### SalesOrderItem

- **Table:** `SalesOrderItem`
- **Definition:**

```prisma
model SalesOrderItem {
  id           String     @id @default(cuid())
  tenantId     String
  salesOrderId String
  salesOrder   SalesOrder @relation(fields: [salesOrderId], references: [id])
  productId    String
  product      Product    @relation(fields: [productId], references: [id])
  quantity     Int
  unitPrice    Float
  total        Float

  @@index([tenantId])
  @@map("sales_order_items")

}
```

### Project

- **Table:** `Project`
- **Definition:**

```prisma
model Project {
  id          String    @id @default(cuid())
  tenantId    String
  name        String
  description String?
  managerId   String
  manager     User      @relation("ProjectManager", fields: [managerId], references: [id])
  status      String    @default("PLANNING") // PLANNING, ACTIVE, ON_HOLD, COMPLETED, CANCELLED
  startDate   DateTime?
  endDate     DateTime?
  budget      Float     @default(0)
  spent       Float     @default(0)
  progress    Int       @default(0)
  createdAt   DateTime  @default(now())

  tasks ProjectTask[]

  @@index([tenantId])
  @@map("projects")

}
```

### ProjectTask

- **Table:** `ProjectTask`
- **Definition:**

```prisma
model ProjectTask {
  id             String    @id @default(cuid())
  tenantId       String
  projectId      String
  project        Project   @relation(fields: [projectId], references: [id])
  title          String
  description    String?
  assigneeId     String?
  assignee       User?     @relation("TaskAssignee", fields: [assigneeId], references: [id])
  status         String    @default("TODO") // TODO, IN_PROGRESS, REVIEW, DONE
  priority       String    @default("MEDIUM") // LOW, MEDIUM, HIGH, URGENT
  dueDate        DateTime?
  estimatedHours Float?
  actualHours    Float     @default(0)
  createdAt      DateTime  @default(now())

  timeEntries TimeEntry[]

  @@index([tenantId])
  @@map("project_tasks")

}
```

### TimeEntry

- **Table:** `TimeEntry`
- **Definition:**

```prisma
model TimeEntry {
  id          String      @id @default(cuid())
  tenantId    String
  taskId      String
  task        ProjectTask @relation(fields: [taskId], references: [id])
  userId      String
  user        User        @relation(fields: [userId], references: [id])
  hours       Float
  date        DateTime
  description String?
  billable    Boolean     @default(true)

  @@index([tenantId])
  @@map("time_entries")

}
```

### Department

- **Table:** `Department`
- **Definition:**

```prisma
model Department {
  id          String   @id @default(cuid())
  tenantId    String
  name        String
  managerId   String?
  manager     User?    @relation("DeptManager", fields: [managerId], references: [id])
  description String?
  budget      Float?
  createdAt   DateTime @default(now())

  employees          Employee[]
  jobs               Job[]
  onboardChecklists  OnboardingChecklist[]
  budgets            Budget[]
  positions          Position[]

  @@index([tenantId])
  @@map("departments")

}
```

### Employee

- **Table:** `Employee`
- **Definition:**

```prisma
model Employee {
  id             String    @id @default(cuid())
  tenantId       String
  userId         String?
  employeeNumber String    @unique
  firstName      String
  lastName       String
  email          String
  phone          String?
  departmentId   String?
  department     Department? @relation(fields: [departmentId], references: [id])
  position       String?
  positionId     String?
  positionRef    Position? @relation(fields: [positionId], references: [id])
  hireDate       DateTime
  salary         Float?
  status         String    @default("ACTIVE") // ACTIVE, ON_LEAVE, TERMINATED
  managerId      String?
  manager        Employee? @relation("EmployeeManager", fields: [managerId], references: [id])
  directReports  Employee[] @relation("EmployeeManager")
  createdAt      DateTime  @default(now())

  leaveRequests       LeaveRequest[]
  payrolls            Payroll[]
  attendances         Attendance[]
  assets              Asset[]
  onboardingTasks     OnboardingTask[]
  trainingEnrollments TrainingEnrollment[]

  @@index([tenantId])
  @@map("employees")

}
```

### LeaveRequest

- **Table:** `LeaveRequest`
- **Definition:**

```prisma
model LeaveRequest {
  id           String    @id @default(cuid())
  tenantId     String
  employeeId   String
  employee     Employee  @relation(fields: [employeeId], references: [id])
  type         String    // ANNUAL, SICK, PERSONAL, MATERNITY, UNPAID
  startDate    DateTime
  endDate      DateTime
  days         Float
  status       String    @default("PENDING") // PENDING, APPROVED, REJECTED
  reason       String?
  approvedById String?
  approvedBy   User?     @relation("LeaveApprover", fields: [approvedById], references: [id])
  createdAt    DateTime  @default(now())

  @@index([tenantId])
  @@map("leave_requests")

}
```

### Payroll

- **Table:** `Payroll`
- **Definition:**

```prisma
model Payroll {
  id          String    @id @default(cuid())
  tenantId    String
  employeeId  String
  employee    Employee  @relation(fields: [employeeId], references: [id])
  period      String
  basicSalary Float
  overtime    Float     @default(0)
  deductions  Float     @default(0)
  bonuses     Float     @default(0)
  tax         Float     @default(0)
  netPay      Float
  status      String    @default("DRAFT") // DRAFT, PROCESSED, PAID
  paidDate    DateTime?
  createdAt   DateTime  @default(now())

  @@index([tenantId])
  @@map("payrolls")

}
```

### Attendance

- **Table:** `Attendance`
- **Definition:**

```prisma
model Attendance {
  id         String   @id @default(cuid())
  tenantId   String
  employeeId String
  employee   Employee @relation(fields: [employeeId], references: [id])
  date       DateTime
  checkIn    String?
  checkOut   String?
  status     String   @default("PRESENT") // PRESENT, ABSENT, LATE, HALF_DAY
  notes      String?

  @@index([tenantId])
  @@map("attendances")

}
```

### Asset

- **Table:** `Asset`
- **Definition:**

```prisma
model Asset {
  id               String    @id @default(cuid())
  tenantId         String
  name             String
  assetTag         String    @unique
  category         String?
  status           String    @default("ACTIVE") // ACTIVE, MAINTENANCE, RETIRED, DISPOSED
  purchaseDate     DateTime?
  purchasePrice    Float?
  currentValue     Float?
  location         String?
  assignedToId     String?
  assignedTo       Employee? @relation(fields: [assignedToId], references: [id])
  depreciationRate Float?
  warrantyExpiry   DateTime?
  createdAt        DateTime  @default(now())

  maintenances AssetMaintenance[]

  @@index([tenantId])
  @@map("assets")

}
```

### AssetMaintenance

- **Table:** `AssetMaintenance`
- **Definition:**

```prisma
model AssetMaintenance {
  id            String    @id @default(cuid())
  tenantId      String
  assetId       String
  asset         Asset     @relation(fields: [assetId], references: [id])
  type          String    // PREVENTIVE, CORRECTIVE
  description   String
  scheduledDate DateTime
  completedDate DateTime?
  cost          Float?
  status        String    @default("SCHEDULED") // SCHEDULED, IN_PROGRESS, COMPLETED, CANCELLED

  @@index([tenantId])
  @@map("asset_maintenances")

}
```

### BillOfMaterials

- **Table:** `BillOfMaterials`
- **Definition:**

```prisma
model BillOfMaterials {
  id        String   @id @default(cuid())
  tenantId  String
  productId String
  product   Product  @relation(fields: [productId], references: [id])
  name      String
  version   String   @default("1.0")
  status    String   @default("DRAFT") // DRAFT, ACTIVE, OBSOLETE
  createdAt DateTime @default(now())

  items      BOMItem[]
  workOrders WorkOrder[]

  @@index([tenantId])
  @@map("bill_of_materials")

}
```

### BOMItem

- **Table:** `BOMItem`
- **Definition:**

```prisma
model BOMItem {
  id             String          @id @default(cuid())
  tenantId       String
  bomId          String
  bom            BillOfMaterials @relation(fields: [bomId], references: [id])
  materialId     String
  material       Product         @relation(fields: [materialId], references: [id])
  quantity       Float
  unit           String          @default("pcs")
  wastagePercent Float           @default(0)

  @@index([tenantId])
  @@map("bom_items")

}
```

### WorkOrder

- **Table:** `WorkOrder`
- **Definition:**

```prisma
model WorkOrder {
  id        String    @id @default(cuid())
  tenantId  String
  bomId     String
  bom       BillOfMaterials @relation(fields: [bomId], references: [id])
  quantity  Int
  status    String    @default("PLANNED") // PLANNED, IN_PROGRESS, COMPLETED, CANCELLED
  startDate DateTime?
  endDate   DateTime?
  priority  String    @default("MEDIUM")
  notes     String?
  createdAt DateTime  @default(now())

  @@index([tenantId])
  @@map("work_orders")

}
```

### Lead

- **Table:** `Lead`
- **Definition:**

```prisma
model Lead {
  id                 String    @id @default(cuid())
  tenantId           String
  firstName          String
  lastName           String
  email              String?
  phone              String?
  company            String?
  source             String    @default("WEB") // WEB, REFERRAL, SOCIAL, ADS, COLD_CALL, EVENT
  status             String    @default("NEW") // NEW, CONTACTED, QUALIFIED, UNQUALIFIED, CONVERTED
  assignedToId       String?
  assignedTo         User?     @relation(fields: [assignedToId], references: [id])
  score              Int       @default(0)
  value              Float?
  notes              String?
  convertedAccountId String?
  convertedDate      DateTime?
  createdAt          DateTime  @default(now())

  opportunities Opportunity[]

  @@index([tenantId])
  @@map("leads")

}
```

### Opportunity

- **Table:** `Opportunity`
- **Definition:**

```prisma
model Opportunity {
  id                String    @id @default(cuid())
  tenantId          String
  title             String
  accountId         String?
  account           Account?  @relation(fields: [accountId], references: [id])
  contactId         String?
  contact           Contact?  @relation(fields: [contactId], references: [id])
  leadId            String?
  lead              Lead?     @relation(fields: [leadId], references: [id])
  stage             String    @default("PROSPECTING") // PROSPECTING, QUALIFICATION, PROPOSAL, NEGOTIATION, CLOSED_WON, CLOSED_LOST
  value             Float     @default(0)
  probability       Int       @default(50)
  expectedCloseDate DateTime?
  assignedToId      String?
  assignedTo        User?     @relation(fields: [assignedToId], references: [id])
  notes             String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@index([tenantId])
  @@map("opportunities")

}
```

### Activity

- **Table:** `Activity`
- **Definition:**

```prisma
model Activity {
  id          String   @id @default(cuid())
  tenantId    String
  type        String   // CALL, EMAIL, MEETING, TASK, NOTE
  subject     String
  description String?
  relatedType String?
  relatedId   String?
  date        DateTime
  duration    Int?     // minutes
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  status      String   @default("COMPLETED")
  createdAt   DateTime @default(now())

  @@index([tenantId])
  @@map("activities")

}
```

### Account

- **Table:** `Account`
- **Definition:**

```prisma
model Account {
  id            String   @id @default(cuid())
  tenantId      String
  name          String
  industry      String?
  website       String?
  phone         String?
  email         String?
  address       String?
  city          String?
  country       String?
  type          String   @default("PROSPECT") // CUSTOMER, PROSPECT, PARTNER, VENDOR
  annualRevenue Float?
  employeeCount Int?
  ownerId       String?
  owner         User?    @relation(fields: [ownerId], references: [id])
  latitude      Float?
  longitude     Float?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  contacts       Contact[]
  invoices       Invoice[]
  salesOrders    SalesOrder[]
  opportunities  Opportunity[]
  deals          Deal[]
  tickets        Ticket[]
  fieldVisits    FieldVisit[]
  loyaltyMembers LoyaltyMember[]

  @@index([tenantId])
  @@map("accounts")

}
```

### Contact

- **Table:** `Contact`
- **Definition:**

```prisma
model Contact {
  id         String   @id @default(cuid())
  tenantId   String
  firstName  String
  lastName   String
  email      String?
  phone      String?
  mobile     String?
  title      String?
  accountId  String?
  account    Account? @relation(fields: [accountId], references: [id])
  ownerId    String?
  owner      User?    @relation(fields: [ownerId], references: [id])
  doNotCall  Boolean  @default(false)
  doNotEmail Boolean  @default(false)
  createdAt  DateTime @default(now())

  opportunities  Opportunity[]
  deals          Deal[]
  tickets        Ticket[]
  fieldVisits    FieldVisit[]
  loyaltyMembers LoyaltyMember[]

  @@index([tenantId])
  @@map("contacts")

}
```

### Campaign

- **Table:** `Campaign`
- **Definition:**

```prisma
model Campaign {
  id              String    @id @default(cuid())
  tenantId        String
  name            String
  type            String    // EMAIL, EVENT, SOCIAL, ADS, WEBINAR, CONTENT
  status          String    @default("DRAFT") // DRAFT, SCHEDULED, ACTIVE, PAUSED, COMPLETED
  startDate       DateTime?
  endDate         DateTime?
  budget          Float     @default(0)
  spent           Float     @default(0)
  expectedRevenue Float     @default(0)
  actualRevenue   Float     @default(0)
  leads           Int       @default(0)
  conversions     Int       @default(0)
  ownerId         String?
  owner           User?     @relation(fields: [ownerId], references: [id])
  createdAt       DateTime  @default(now())

  @@index([tenantId])
  @@map("campaigns")

}
```

### Ticket

- **Table:** `Ticket`
- **Definition:**

```prisma
model Ticket {
  id           String    @id @default(cuid())
  tenantId     String
  ticketNumber String    @unique
  subject      String
  description  String
  accountId    String?
  account      Account?  @relation(fields: [accountId], references: [id])
  contactId    String?
  contact      Contact?  @relation(fields: [contactId], references: [id])
  status       String    @default("OPEN") // OPEN, IN_PROGRESS, WAITING, ESCALATED, RESOLVED, CLOSED
  priority     String    @default("MEDIUM") // LOW, MEDIUM, HIGH, CRITICAL
  assignedToId String?
  assignedTo   User?     @relation(fields: [assignedToId], references: [id])
  category     String?
  slaDeadline  DateTime?
  resolvedAt   DateTime?
  satisfaction Int?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@index([tenantId])
  @@map("tickets")

}
```

### KnowledgeArticle

- **Table:** `KnowledgeArticle`
- **Definition:**

```prisma
model KnowledgeArticle {
  id         String   @id @default(cuid())
  tenantId   String
  title      String
  content    String
  category   String?
  tags       String?
  status     String   @default("DRAFT") // DRAFT, PUBLISHED, ARCHIVED
  views      Int      @default(0)
  helpful    Int      @default(0)
  notHelpful Int      @default(0)
  authorId   String
  author     User     @relation(fields: [authorId], references: [id])
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([tenantId])
  @@map("knowledge_articles")

}
```

### FieldVisit

- **Table:** `FieldVisit`
- **Definition:**

```prisma
model FieldVisit {
  id           String    @id @default(cuid())
  tenantId     String
  userId       String
  user         User      @relation(fields: [userId], references: [id])
  accountId    String?
  account      Account?  @relation(fields: [accountId], references: [id])
  contactId    String?
  contact      Contact?  @relation(fields: [contactId], references: [id])
  checkInTime  DateTime
  checkOutTime DateTime?
  latitude     Float
  longitude    Float
  address      String?
  notes        String?
  status       String    @default("PLANNED") // PLANNED, IN_PROGRESS, COMPLETED, CANCELLED
  distance     Float?
  createdAt    DateTime  @default(now())

  @@index([tenantId])
  @@map("field_visits")

}
```

### GPSLocation

- **Table:** `GPSLocation`
- **Definition:**

```prisma
model GPSLocation {
  id           String   @id @default(cuid())
  tenantId     String
  userId       String
  user         User     @relation(fields: [userId], references: [id])
  latitude     Float
  longitude    Float
  accuracy     Float?
  timestamp    DateTime @default(now())
  batteryLevel Int?

  @@index([tenantId])
  @@map("gps_locations")

}
```

### Territory

- **Table:** `Territory`
- **Definition:**

```prisma
model Territory {
  id           String          @id @default(cuid())
  tenantId     String
  name         String
  nameAr       String?
  code         String?         @unique
  description  String?
  level        TerritoryLevel  @default(REGION)
  parentId     String?
  parent       Territory?      @relation("TerritoryHierarchy", fields: [parentId], references: [id])
  children     Territory[]     @relation("TerritoryHierarchy")
  imsCode      String?
  geoShare     Float?
  assignedToId String?
  assignedTo   User?           @relation(fields: [assignedToId], references: [id])
  boundaries   String?         // JSON polygon for geo-fencing
  color        String?
  createdAt    DateTime        @default(now())

  businessUnits BusinessUnitTerritory[]
  doctors       Doctor[]

  @@index([tenantId])
  @@index([level])
  @@index([parentId])
  @@map("territories")

}
```

### LoyaltyProgram

- **Table:** `LoyaltyProgram`
- **Definition:**

```prisma
model LoyaltyProgram {
  id             String   @id @default(cuid())
  tenantId       String
  name           String
  description    String?
  pointsPerDollar Float   @default(1)
  redemptionRate Float    @default(0.01)
  status         String   @default("ACTIVE") // ACTIVE, INACTIVE
  createdAt      DateTime @default(now())

  members LoyaltyMember[]

  @@index([tenantId])
  @@map("loyalty_programs")

}
```

### LoyaltyMember

- **Table:** `LoyaltyMember`
- **Definition:**

```prisma
model LoyaltyMember {
  id        String         @id @default(cuid())
  tenantId  String
  programId String
  program   LoyaltyProgram @relation(fields: [programId], references: [id])
  accountId String?
  account   Account?       @relation(fields: [accountId], references: [id])
  contactId String?
  contact   Contact?       @relation(fields: [contactId], references: [id])
  points    Int            @default(0)
  tier      String         @default("BRONZE") // BRONZE, SILVER, GOLD, PLATINUM
  joinDate  DateTime       @default(now())
  createdAt DateTime       @default(now())

  transactions LoyaltyTransaction[]

  @@index([tenantId])
  @@map("loyalty_members")

}
```

### LoyaltyTransaction

- **Table:** `LoyaltyTransaction`
- **Definition:**

```prisma
model LoyaltyTransaction {
  id          String        @id @default(cuid())
  tenantId    String
  memberId    String
  member      LoyaltyMember @relation(fields: [memberId], references: [id])
  type        String        // EARN, REDEEM, ADJUST, EXPIRE
  points      Int
  description String?
  date        DateTime      @default(now())

  @@index([tenantId])
  @@map("loyalty_transactions")

}
```

### Job

- **Table:** `Job`
- **Definition:**

```prisma
model Job {
  id              String    @id @default(cuid())
  tenantId        String
  title           String
  departmentId    String?
  department      Department? @relation(fields: [departmentId], references: [id])
  location        String?
  type            String    @default("FULL_TIME") // FULL_TIME, PART_TIME, CONTRACT, INTERN, REMOTE
  status          String    @default("DRAFT") // DRAFT, OPEN, ON_HOLD, CLOSED
  description     String?
  requirements    String?
  salaryMin       Float?
  salaryMax       Float?
  benefits        String?
  postedDate      DateTime?
  closingDate     DateTime?
  hiringManagerId String?
  hiringManager   User?     @relation(fields: [hiringManagerId], references: [id])
  createdAt       DateTime  @default(now())

  applications Application[]

  @@index([tenantId])
  @@map("jobs")

}
```

### Candidate

- **Table:** `Candidate`
- **Definition:**

```prisma
model Candidate {
  id             String   @id @default(cuid())
  tenantId       String
  firstName      String
  lastName       String
  email          String
  phone          String?
  linkedIn       String?
  resumeUrl      String?
  source         String   @default("WEBSITE") // WEBSITE, LINKEDIN, REFERRAL, AGENCY, JOB_BOARD
  status         String   @default("NEW") // NEW, SCREENING, SHORTLISTED, INTERVIEW, OFFER, HIRED, REJECTED
  currentCompany String?
  currentTitle   String?
  expectedSalary Float?
  rating         Int?
  notes          String?
  createdAt      DateTime @default(now())

  applications Application[]

  @@index([tenantId])
  @@map("candidates")

}
```

### Application

- **Table:** `Application`
- **Definition:**

```prisma
model Application {
  id          String    @id @default(cuid())
  tenantId    String
  candidateId String
  candidate   Candidate @relation(fields: [candidateId], references: [id])
  jobId       String
  job         Job       @relation(fields: [jobId], references: [id])
  status      String    @default("APPLIED") // APPLIED, SCREENING, SHORTLISTED, INTERVIEW, OFFER, HIRED, REJECTED
  appliedDate DateTime  @default(now())
  coverLetter String?
  score       Int?
  createdAt   DateTime  @default(now())

  interviews  Interview[]
  offerLetter OfferLetter?

  @@index([tenantId])
  @@map("applications")

}
```

### Interview

- **Table:** `Interview`
- **Definition:**

```prisma
model Interview {
  id             String      @id @default(cuid())
  tenantId       String
  applicationId  String
  application    Application @relation(fields: [applicationId], references: [id])
  interviewerIds String      // comma-separated user IDs
  scheduledDate  DateTime
  duration       Int         @default(60) // minutes
  type           String      @default("VIDEO") // PHONE, VIDEO, ONSITE, PANEL, TECHNICAL
  status         String      @default("SCHEDULED") // SCHEDULED, COMPLETED, CANCELLED, NO_SHOW
  feedback       String?
  rating         Int?
  location       String?
  createdAt      DateTime    @default(now())

  @@index([tenantId])
  @@map("interviews")

}
```

### OfferLetter

- **Table:** `OfferLetter`
- **Definition:**

```prisma
model OfferLetter {
  id            String      @id @default(cuid())
  tenantId      String
  applicationId String      @unique
  application   Application @relation(fields: [applicationId], references: [id])
  salary        Float
  startDate     DateTime
  expiryDate    DateTime?
  benefits      String?
  status        String      @default("DRAFT") // DRAFT, SENT, ACCEPTED, DECLINED, EXPIRED
  terms         String?
  createdAt     DateTime    @default(now())

  @@index([tenantId])
  @@map("offer_letters")

}
```

### OnboardingChecklist

- **Table:** `OnboardingChecklist`
- **Definition:**

```prisma
model OnboardingChecklist {
  id           String      @id @default(cuid())
  tenantId     String
  name         String
  departmentId String?
  department   Department? @relation(fields: [departmentId], references: [id])
  items        String      @default("[]") // JSON array
  createdAt    DateTime    @default(now())

  tasks OnboardingTask[]

  @@index([tenantId])
  @@map("onboarding_checklists")

}
```

### OnboardingTask

- **Table:** `OnboardingTask`
- **Definition:**

```prisma
model OnboardingTask {
  id           String               @id @default(cuid())
  tenantId     String
  checklistId  String?
  checklist    OnboardingChecklist?  @relation(fields: [checklistId], references: [id])
  employeeId   String
  employee     Employee             @relation(fields: [employeeId], references: [id])
  title        String
  description  String?
  assignedToId String?
  assignedTo   User?                @relation("OnboardAssigner", fields: [assignedToId], references: [id])
  dueDate      DateTime?
  status       String               @default("PENDING") // PENDING, IN_PROGRESS, COMPLETED
  category     String               @default("ORIENTATION") // PAPERWORK, IT_SETUP, TRAINING, ORIENTATION, COMPLIANCE
  createdAt    DateTime             @default(now())

  @@index([tenantId])
  @@map("onboarding_tasks")

}
```

### TrainingCourse

- **Table:** `TrainingCourse`
- **Definition:**

```prisma
model TrainingCourse {
  id          String   @id @default(cuid())
  tenantId    String
  title       String
  description String?
  category    String?
  duration    String?
  format      String   @default("ONLINE") // ONLINE, CLASSROOM, HYBRID
  status      String   @default("DRAFT") // DRAFT, PUBLISHED, ARCHIVED
  materials   String?
  createdAt   DateTime @default(now())

  enrollments TrainingEnrollment[]

  @@index([tenantId])
  @@map("training_courses")

}
```

### TrainingEnrollment

- **Table:** `TrainingEnrollment`
- **Definition:**

```prisma
model TrainingEnrollment {
  id            String    @id @default(cuid())
  tenantId      String
  courseId       String
  course        TrainingCourse @relation(fields: [courseId], references: [id])
  employeeId    String
  employee      Employee  @relation(fields: [employeeId], references: [id])
  status        String    @default("ENROLLED") // ENROLLED, IN_PROGRESS, COMPLETED, DROPPED
  enrolledDate  DateTime  @default(now())
  completedDate DateTime?
  score         Int?

  @@index([tenantId])
  @@map("training_enrollments")

}
```

### BusinessUnit

- **Table:** `BusinessUnit`
- **Definition:**

```prisma
model BusinessUnit {
  id          String   @id @default(cuid())
  tenantId    String
  name        String
  code        String   @unique
  description String?
  managerId   String
  manager     User     @relation("BUManager", fields: [managerId], references: [id])
  color       String?
  status      String   @default("ACTIVE") // ACTIVE, INACTIVE
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  members     BusinessUnitMember[]
  products    BusinessUnitProduct[]
  territories BusinessUnitTerritory[]
  approvalLogs ApprovalLog[]
  doctors      Doctor[]

  @@index([tenantId])
  @@map("business_units")

}
```

### BusinessUnitMember

- **Table:** `BusinessUnitMember`
- **Definition:**

```prisma
model BusinessUnitMember {
  id             String       @id @default(cuid())
  tenantId       String
  businessUnitId String
  businessUnit   BusinessUnit @relation(fields: [businessUnitId], references: [id])
  userId         String
  user           User         @relation(fields: [userId], references: [id])
  role           String       @default("MEDICAL_REP") // BUM, MARKETEER, DISTRICT_MANAGER, MEDICAL_REP
  assignedAt     DateTime     @default(now())

  @@unique([businessUnitId, userId])
  @@index([tenantId])
  @@map("business_unit_members")

}
```

### BusinessUnitProduct

- **Table:** `BusinessUnitProduct`
- **Definition:**

```prisma
model BusinessUnitProduct {
  id             String       @id @default(cuid())
  tenantId       String
  businessUnitId String
  businessUnit   BusinessUnit @relation(fields: [businessUnitId], references: [id])
  productId      String
  product        Product      @relation(fields: [productId], references: [id])
  assignedAt     DateTime     @default(now())

  @@unique([businessUnitId, productId])
  @@index([tenantId])
  @@map("business_unit_products")

}
```

### BusinessUnitTerritory

- **Table:** `BusinessUnitTerritory`
- **Definition:**

```prisma
model BusinessUnitTerritory {
  id             String       @id @default(cuid())
  tenantId       String
  businessUnitId String
  businessUnit   BusinessUnit @relation(fields: [businessUnitId], references: [id])
  territoryId    String
  territory      Territory    @relation(fields: [territoryId], references: [id])
  assignedAt     DateTime     @default(now())

  @@unique([businessUnitId, territoryId])
  @@index([tenantId])
  @@map("business_unit_territories")

}
```

### ApprovalLog

- **Table:** `ApprovalLog`
- **Definition:**

```prisma
model ApprovalLog {
  id             String        @id @default(cuid())
  tenantId       String
  entityType     String        // WEEKLY_PLAN, EXPENSE, MARKET_REQUEST, STRATEGIC
  entityId       String
  action         String        // SUBMITTED, APPROVED, REJECTED, ESCALATED, AUTO_ESCALATED
  fromStatus     String
  toStatus       String
  performedById  String
  performedBy    User          @relation("ApprovalPerformer", fields: [performedById], references: [id])
  comment        String?
  level          Int           @default(1) // 1=DM, 2=Marketeer, 3=BUM
  businessUnitId String?
  businessUnit   BusinessUnit? @relation(fields: [businessUnitId], references: [id])
  createdAt      DateTime      @default(now())

  @@index([tenantId])
  @@index([entityType, entityId])
  @@map("approval_logs")

}
```

### Document

- **Table:** `Document`
- **Definition:**

```prisma
model Document {
  id           String   @id @default(cuid())
  tenantId     String
  name         String
  type         String?
  category     String?
  fileUrl      String
  fileSize     Int?
  uploadedById String
  uploadedBy   User     @relation(fields: [uploadedById], references: [id])
  module       String?
  entityType   String?
  entityId     String?
  version      Int      @default(1)
  tags         String?
  createdAt    DateTime @default(now())

  @@index([tenantId])
  @@index([module, entityType, entityId])
  @@map("documents")

}
```

### Doctor

- **Table:** `Doctor`
- **Definition:**

```prisma
model Doctor {
  id                 String              @id @default(cuid())
  tenantId           String
  name               String
  specialty          String              // IMS specialty (General Practice, Cardiology, etc.)
  hospital           String?
  city               String?
  phone              String?
  email              String?
  classification     DoctorClassification @default(B)
  potentialRevenue   Float?              // estimated monthly Rx value
  areaWeight         Int?                // 1-10 importance scale
  isKOL              Boolean             @default(false) // Key Opinion Leader
  buyingLadderStage  BuyingLadderStage   @default(UNAWARE)
  visitFrequency     Int                 @default(2) // required visits per month
  lastVisitAt        DateTime?
  notes              String?
  lat                Float?
  lng                Float?
  assignedRepId      String?
  assignedRep        User?               @relation("DoctorRep", fields: [assignedRepId], references: [id])
  buId               String?
  businessUnit       BusinessUnit?       @relation(fields: [buId], references: [id])
  brickId            String?
  territory          Territory?          @relation(fields: [brickId], references: [id])
  linkedPharmacyIds  String?             // JSON array of pharmacy AMAccount IDs
  linkedAccountIds   String?             // JSON array of hospital/polyclinic IDs
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt

  visits          Visit[]
  marketRequests  MarketRequest[]

  @@index([tenantId])
  @@index([assignedRepId])
  @@index([buId])
  @@index([classification])
  @@index([specialty])
  @@map("doctors")

}
```

### Visit

- **Table:** `Visit`
- **Definition:**

```prisma
model Visit {
  id                  String        @id @default(cuid())
  tenantId            String
  repId               String
  rep                 User          @relation("VisitRep", fields: [repId], references: [id])
  doctorId            String
  doctor              Doctor        @relation(fields: [doctorId], references: [id])
  amAccountId         String?
  dateTime            DateTime
  session             VisitSession  @default(AM)
  type                VisitType     @default(SINGLE)
  partnerId           String?       // for DOUBLE visits
  durationMin         Int?
  status              VisitStatus   @default(LOGGED)
  gpsVerified         Boolean       @default(false)
  lat                 Float?
  lng                 Float?
  checkInTime         DateTime?
  checkOutTime        DateTime?
  notes               String?
  feedback            String?
  products            String?       // JSON array of product IDs discussed
  samplesGiven        String?       // JSON array { productId, quantity }
  samplesDistributed  Int           @default(0)
  buyingLadderBefore  String?
  buyingLadderAfter   String?
  activityRequests    String?       // JSON array of activity requests
  buId                String?
  planId              String?
  isUnplanned         Boolean       @default(false)
  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt

  @@index([tenantId])
  @@index([repId, dateTime])
  @@index([doctorId])
  @@index([status])
  @@index([buId])
  @@map("visits")

}
```

### WeeklyPlan

- **Table:** `WeeklyPlan`
- **Definition:**

```prisma
model WeeklyPlan {
  id              String     @id @default(cuid())
  tenantId        String
  repId           String
  rep             User       @relation("PlanRep", fields: [repId], references: [id])
  weekStartDate   DateTime
  days            String     // JSON array of DailyPlan objects
  status          PlanStatus @default(DRAFT)
  submittedAt     DateTime?
  approvedById    String?
  approvedBy      User?      @relation("PlanApprover", fields: [approvedById], references: [id])
  approvedAt      DateTime?
  rejectionReason String?
  notes           String?
  approvalHistory String?    // JSON array of ApprovalEntry objects
  approvalLevel   Int?
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt

  @@index([tenantId])
  @@index([repId, weekStartDate])
  @@index([status])
  @@map("weekly_plans")

}
```

### MarketRequest

- **Table:** `MarketRequest`
- **Definition:**

```prisma
model MarketRequest {
  id              String        @id @default(cuid())
  tenantId        String
  type            RequestType
  requestedById   String
  requestedBy     User          @relation("RequestedBy", fields: [requestedById], references: [id])
  doctorId        String?
  doctor          Doctor?       @relation(fields: [doctorId], references: [id])
  productId       String?
  description     String
  quantity        Int?
  amount          Float?
  priority        TaskPriority  @default(MEDIUM)
  status          RequestStatus @default(PENDING)
  approvedById    String?
  approvedBy      User?         @relation("RequestApprover", fields: [approvedById], references: [id])
  approvedAt      DateTime?
  rejectionReason String?
  buId            String?
  proposedChanges String?       // JSON for DOCTOR_EDIT requests
  targetEntityId  String?       // e.g., the doctor id being edited
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@index([tenantId])
  @@index([requestedById])
  @@index([status])
  @@index([buId])
  @@map("market_requests")

}
```

### Expense

- **Table:** `Expense`
- **Definition:**

```prisma
model Expense {
  id             String        @id @default(cuid())
  tenantId       String
  type           String        // TRAVEL, MEALS, CONFERENCE, OFFICE, SAMPLES, etc.
  amount         Float
  date           DateTime
  description    String?
  status         ExpenseStatus @default(DRAFT)
  submittedById  String
  submittedBy    User          @relation("ExpenseSubmitter", fields: [submittedById], references: [id])
  approvedById   String?
  approvedBy     User?         @relation("ExpenseApprover", fields: [approvedById], references: [id])
  receiptUrl     String?
  startOdometer  Float?
  endOdometer    Float?
  totalKm        Float?
  ratePerKm      Float?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  @@index([tenantId])
  @@index([submittedById])
  @@index([status])
  @@map("expenses")

}
```

### KPI

- **Table:** `KPI`
- **Definition:**

```prisma
model KPI {
  id        String   @id @default(cuid())
  tenantId  String
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  metric    String   // e.g., "Visits", "New Doctors", "Market Requests"
  value     Float    @default(0)
  target    Float    @default(0)
  period    String   // YYYY-MM format
  setById   String?  // userId of the superior who set the target
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([tenantId])
  @@index([userId, period])
  @@index([metric])
  @@map("kpis")

}
```

### StandaloneTask

- **Table:** `StandaloneTask`
- **Definition:**

```prisma
model StandaloneTask {
  id           String       @id @default(cuid())
  tenantId     String
  title        String
  description  String?
  assignedById String
  assignedBy   User         @relation("StandaloneTaskCreator", fields: [assignedById], references: [id])
  assignedToId String
  assignedTo   User         @relation("StandaloneTaskAssignee", fields: [assignedToId], references: [id])
  dueDate      DateTime?
  status       TaskStatus   @default(TODO)
  priority     TaskPriority @default(MEDIUM)
  kpiMetric    String?
  kpiTarget    Float?
  kpiActual    Float?
  buId         String?
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt

  @@index([tenantId])
  @@index([assignedToId])
  @@index([status])
  @@map("standalone_tasks")

}
```

### Message

- **Table:** `Message`
- **Definition:**

```prisma
model Message {
  id          String   @id @default(cuid())
  tenantId    String
  subject     String
  body        String
  senderId    String
  sender      User     @relation("MessageSender", fields: [senderId], references: [id])
  recipientId String
  recipient   User     @relation("MessageRecipient", fields: [recipientId], references: [id])
  channelId   String?
  isRead      Boolean  @default(false)
  starred     Boolean  @default(false)
  createdAt   DateTime @default(now())

  @@index([tenantId])
  @@index([recipientId, isRead])
  @@index([senderId])
  @@map("messages")

}
```

### QAudit

- **Table:** `QAudit`
- **Definition:**

```prisma
model QAudit {
  id            String      @id @default(cuid())
  tenantId      String
  title         String
  type          AuditType
  status        AuditStatus @default(PLANNED)
  auditor       String
  auditee       String
  department    String?
  scheduledDate DateTime
  completedDate DateTime?
  score         Float?
  notes         String?
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  findings QAuditFinding[]

  @@index([tenantId])
  @@map("q_audits")

}
```

### QAuditFinding

- **Table:** `QAuditFinding`
- **Definition:**

```prisma
model QAuditFinding {
  id          String          @id @default(cuid())
  tenantId    String
  auditId     String
  audit       QAudit          @relation(fields: [auditId], references: [id])
  title       String
  description String?
  severity    FindingSeverity
  status      FindingStatus   @default(OPEN)
  assignedTo  String?
  dueDate     DateTime?
  evidence    String?
  capaId      String?
  capa        CAPA?           @relation(fields: [capaId], references: [id])
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

  @@index([tenantId])
  @@index([auditId])
  @@map("q_audit_findings")

}
```

### BatchRelease

- **Table:** `BatchRelease`
- **Definition:**

```prisma
model BatchRelease {
  id            String             @id @default(cuid())
  tenantId      String
  batchNumber   String
  productId     String
  product       Product            @relation(fields: [productId], references: [id])
  status        BatchReleaseStatus @default(PENDING)
  reviewedBy    String?
  approvedBy    String?
  releaseDate   DateTime?
  expiryDate    DateTime?
  qaDecision    String?
  notes         String?
  signatureHash String?
  createdAt     DateTime           @default(now())
  updatedAt     DateTime           @updatedAt

  @@index([tenantId])
  @@map("batch_releases")

}
```

### CAPA

- **Table:** `CAPA`
- **Definition:**

```prisma
model CAPA {
  id                 String     @id @default(cuid())
  tenantId           String
  number             String
  title              String
  type               CAPAType
  status             CAPAStatus @default(OPEN)
  priority           CAPAPriority
  source             String?
  description        String?
  rootCause          String?
  assignedTo         String?
  dueDate            DateTime?
  completedDate      DateTime?
  effectivenessCheck String?
  createdAt          DateTime   @default(now())
  updatedAt          DateTime   @updatedAt

  actions  CAPAAction[]
  findings QAuditFinding[]

  @@index([tenantId])
  @@map("capas")

}
```

### CAPAAction

- **Table:** `CAPAAction`
- **Definition:**

```prisma
model CAPAAction {
  id            String           @id @default(cuid())
  tenantId      String
  capaId        String
  capa          CAPA             @relation(fields: [capaId], references: [id])
  description   String
  assignedTo    String?
  dueDate       DateTime?
  completedDate DateTime?
  status        CAPAActionStatus @default(PENDING)
  evidence      String?
  createdAt     DateTime         @default(now())
  updatedAt     DateTime         @updatedAt

  @@index([tenantId])
  @@index([capaId])
  @@map("capa_actions")

}
```

### ChangeControl

- **Table:** `ChangeControl`
- **Definition:**

```prisma
model ChangeControl {
  id                 String              @id @default(cuid())
  tenantId           String
  number             String
  title              String
  description        String?
  type               ChangeControlType
  status             ChangeControlStatus @default(INITIATED)
  priority           String?
  requestedBy        String?
  approvedBy         String?
  impactAssessment   String?
  implementationPlan String?
  effectiveDate      DateTime?
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt

  @@index([tenantId])
  @@map("change_controls")

}
```

### CleaningValidation

- **Table:** `CleaningValidation`
- **Definition:**

```prisma
model CleaningValidation {
  id                 String                   @id @default(cuid())
  tenantId           String
  protocol           String
  equipment          String
  area               String?
  status             CleaningValidationStatus @default(PENDING)
  method             String?
  acceptanceCriteria String?
  results            String?
  validatedBy        String?
  validationDate     DateTime?
  nextValidationDate DateTime?
  createdAt          DateTime                 @default(now())
  updatedAt          DateTime                 @updatedAt

  @@index([tenantId])
  @@map("cleaning_validations")

}
```

### QComplaint

- **Table:** `QComplaint`
- **Definition:**

```prisma
model QComplaint {
  id            String          @id @default(cuid())
  tenantId      String
  number        String
  title         String
  source        ComplaintSource
  status        ComplaintStatus @default(RECEIVED)
  priority      String?
  product       String?
  batchNumber   String?
  description   String?
  investigation String?
  rootCause     String?
  capaId        String?
  reportedBy    String?
  reportedDate  DateTime?
  closedDate    DateTime?
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  @@index([tenantId])
  @@map("q_complaints")

}
```

### Deviation

- **Table:** `Deviation`
- **Definition:**

```prisma
model Deviation {
  id               String            @id @default(cuid())
  tenantId         String
  number           String
  title            String
  type             DeviationType
  status           DeviationStatus   @default(OPEN)
  severity         DeviationSeverity
  department       String?
  description      String?
  rootCause        String?
  impactAssessment String?
  immediateAction  String?
  capaId           String?
  reportedBy       String?
  reportedDate     DateTime?
  closedDate       DateTime?
  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt

  @@index([tenantId])
  @@map("deviations")

}
```

### QDocument

- **Table:** `QDocument`
- **Definition:**

```prisma
model QDocument {
  id            String          @id @default(cuid())
  tenantId      String
  number        String
  title         String
  type          QDocumentType
  status        QDocumentStatus @default(DRAFT)
  version       String          @default("1.0")
  department    String?
  author        String?
  reviewer      String?
  approver      String?
  effectiveDate DateTime?
  reviewDate    DateTime?
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  versions QDocumentVersion[]

  @@index([tenantId])
  @@map("q_documents")

}
```

### QDocumentVersion

- **Table:** `QDocumentVersion`
- **Definition:**

```prisma
model QDocumentVersion {
  id                String    @id @default(cuid())
  tenantId          String
  documentId        String
  document          QDocument @relation(fields: [documentId], references: [id])
  version           String
  content           String?
  changeDescription String?
  author            String?
  approvedBy        String?
  approvedDate      DateTime?
  createdAt         DateTime  @default(now())

  @@index([tenantId])
  @@index([documentId])
  @@map("q_document_versions")

}
```

### EnvironmentalMonitoring

- **Table:** `EnvironmentalMonitoring`
- **Definition:**

```prisma
model EnvironmentalMonitoring {
  id           String                 @id @default(cuid())
  tenantId     String
  area         String
  parameter    EnvironmentalParameter
  value        Float
  unit         String
  minLimit     Float?
  maxLimit     Float?
  status       MonitoringStatus       @default(WITHIN_SPEC)
  readingDate  DateTime
  recordedBy   String?
  equipment    String?
  notes        String?
  createdAt    DateTime               @default(now())
  updatedAt    DateTime               @updatedAt

  @@index([tenantId])
  @@map("environmental_monitorings")

}
```

### OOSInvestigation

- **Table:** `OOSInvestigation`
- **Definition:**

```prisma
model OOSInvestigation {
  id             String    @id @default(cuid())
  tenantId       String
  number         String
  title          String
  status         OOSStatus @default(INITIATED)
  batchNumber    String?
  product        String?
  testName       String?
  specification  String?
  result         String?
  assignedTo     String?
  phase1Findings String?
  phase2Findings String?
  conclusion     String?
  capaId         String?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([tenantId])
  @@map("oos_investigations")

}
```

### Recall

- **Table:** `Recall`
- **Definition:**

```prisma
model Recall {
  id                String       @id @default(cuid())
  tenantId          String
  number            String
  title             String
  type              RecallType
  status            RecallStatus @default(INITIATED)
  product           String?
  batchNumbers      String?
  reason            String?
  affectedQuantity  Int?
  distributionScope String?
  healthHazard      String?
  strategy          String?
  effectiveness     String?
  initiatedBy       String?
  initiatedDate     DateTime?
  closedDate        DateTime?
  createdAt         DateTime     @default(now())
  updatedAt         DateTime     @updatedAt

  @@index([tenantId])
  @@map("recalls")

}
```

### RiskAssessment

- **Table:** `RiskAssessment`
- **Definition:**

```prisma
model RiskAssessment {
  id               String               @id @default(cuid())
  tenantId         String
  title            String
  type             RiskAssessmentType
  status           RiskAssessmentStatus  @default(DRAFT)
  methodology      RiskMethodology
  scope            String?
  team             String?
  hazards          Json?
  overallRiskLevel RiskLevel?
  mitigations      String?
  reviewDate       DateTime?
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt

  @@index([tenantId])
  @@map("risk_assessments")

}
```

### SPCChart

- **Table:** `SPCChart`
- **Definition:**

```prisma
model SPCChart {
  id          String       @id @default(cuid())
  tenantId    String
  name        String
  type        SPCChartType
  parameter   String?
  product     String?
  specification String?
  ucl         Float
  lcl         Float
  centerLine  Float
  status      SPCStatus    @default(ACTIVE)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  dataPoints SPCDataPoint[]

  @@index([tenantId])
  @@map("spc_charts")

}
```

### SPCDataPoint

- **Table:** `SPCDataPoint`
- **Definition:**

```prisma
model SPCDataPoint {
  id           String   @id @default(cuid())
  tenantId     String
  chartId      String
  chart        SPCChart @relation(fields: [chartId], references: [id])
  value        Float
  sampleSize   Int?
  timestamp    DateTime
  recordedBy   String?
  notes        String?
  outOfControl Boolean  @default(false)
  createdAt    DateTime @default(now())

  @@index([tenantId])
  @@index([chartId])
  @@map("spc_data_points")

}
```

### StabilityStudy

- **Table:** `StabilityStudy`
- **Definition:**

```prisma
model StabilityStudy {
  id         String          @id @default(cuid())
  tenantId   String
  protocol   String
  product    String
  batchNumber String
  condition  String?
  status     StabilityStatus @default(PLANNED)
  startDate  DateTime?
  endDate    DateTime?
  conclusion String?
  createdAt  DateTime        @default(now())
  updatedAt  DateTime        @updatedAt

  timepoints StabilityTimepoint[]

  @@index([tenantId])
  @@map("stability_studies")

}
```

### StabilityTimepoint

- **Table:** `StabilityTimepoint`
- **Definition:**

```prisma
model StabilityTimepoint {
  id            String          @id @default(cuid())
  tenantId      String
  studyId       String
  study         StabilityStudy  @relation(fields: [studyId], references: [id])
  timepoint     String
  scheduledDate DateTime?
  actualDate    DateTime?
  results       Json?
  status        TimepointStatus @default(PENDING)
  analyst       String?
  createdAt     DateTime        @default(now())

  @@index([tenantId])
  @@index([studyId])
  @@map("stability_timepoints")

}
```

### SupplierQuality

- **Table:** `SupplierQuality`
- **Definition:**

```prisma
model SupplierQuality {
  id                String                @id @default(cuid())
  tenantId          String
  supplierId        String
  supplierName      String
  status            SupplierQualityStatus @default(APPROVED)
  qualificationDate DateTime?
  lastAuditDate     DateTime?
  nextAuditDate     DateTime?
  overallScore      Float?
  qualityScore      Float?
  deliveryScore     Float?
  complianceScore   Float?
  issues            Json?
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@index([tenantId])
  @@map("supplier_qualities")

}
```

### TrainingRecord

- **Table:** `TrainingRecord`
- **Definition:**

```prisma
model TrainingRecord {
  id            String               @id @default(cuid())
  tenantId      String
  employeeId    String
  employeeName  String
  trainingType  String
  title         String
  status        TrainingRecordStatus @default(ASSIGNED)
  assignedDate  DateTime?
  dueDate       DateTime?
  completedDate DateTime?
  score         Float?
  trainer       String?
  certificate   String?
  expiryDate    DateTime?
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt

  @@index([tenantId])
  @@map("training_records")

}
```

### VendorScore

- **Table:** `VendorScore`
- **Definition:**

```prisma
model VendorScore {
  id             String   @id @default(cuid())
  tenantId       String
  vendorId       String
  vendorName     String
  period         String
  qualityScore   Float
  deliveryScore  Float
  priceScore     Float
  serviceScore   Float
  overallScore   Float
  evaluatedBy    String?
  evaluationDate DateTime?
  notes          String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([tenantId])
  @@map("vendor_scores")

}
```

### WaterSystemReading

- **Table:** `WaterSystemReading`
- **Definition:**

```prisma
model WaterSystemReading {
  id          String             @id @default(cuid())
  tenantId    String
  system      WaterSystem
  point       String
  parameter   WaterParameter
  value       Float
  unit        String
  limit       Float
  status      WaterReadingStatus @default(PASS)
  readingDate DateTime
  recordedBy  String?
  equipment   String?
  notes       String?
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@index([tenantId])
  @@map("water_system_readings")

}
```

### EquipmentRecord

- **Table:** `EquipmentRecord`
- **Definition:**

```prisma
model EquipmentRecord {
  id                  String          @id @default(cuid())
  tenantId            String
  name                String
  code                String
  type                String?
  department          String?
  status              EquipmentStatus @default(OPERATIONAL)
  manufacturer        String?
  model               String?
  serialNumber        String?
  installDate         DateTime?
  lastMaintenanceDate DateTime?
  nextMaintenanceDate DateTime?
  calibrationDueDate  DateTime?
  location            String?
  notes               String?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt

  @@index([tenantId])
  @@map("equipment_records")

}
```

### HVACReading

- **Table:** `HVACReading`
- **Definition:**

```prisma
model HVACReading {
  id                   String     @id @default(cuid())
  tenantId             String
  zone                 String
  area                 String?
  temperature          Float
  humidity             Float
  differentialPressure Float
  airChanges           Float?
  particleCount        Int?
  status               HVACStatus @default(NORMAL)
  readingDate          DateTime
  recordedBy           String?
  notes                String?
  createdAt            DateTime   @default(now())
  updatedAt            DateTime   @updatedAt

  @@index([tenantId])
  @@map("hvac_readings")

}
```

### ThreeWayMatch

- **Table:** `ThreeWayMatch`
- **Definition:**

```prisma
model ThreeWayMatch {
  id                  String              @id @default(cuid())
  tenantId            String
  purchaseOrderId     String
  goodsReceiptId      String
  invoiceId           String
  status              ThreeWayMatchStatus @default(PENDING)
  poAmount            Float
  grAmount            Float
  invoiceAmount       Float
  variance            Float
  variancePercentage  Float
  resolution          String?
  resolvedBy          String?
  resolvedDate        DateTime?
  createdAt           DateTime            @default(now())
  updatedAt           DateTime            @updatedAt

  @@index([tenantId])
  @@map("three_way_matches")

}
```

### WarehouseZone

- **Table:** `WarehouseZone`
- **Definition:**

```prisma
model WarehouseZone {
  id               String              @id @default(cuid())
  tenantId         String
  name             String
  code             String
  warehouseId      String
  type             WarehouseZoneType
  temperature      Float?
  humidity         Float?
  capacity         Int
  currentOccupancy Int                 @default(0)
  status           WarehouseZoneStatus @default(ACTIVE)
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt

  @@index([tenantId])
  @@map("warehouse_zones")

}
```

### ManufacturingBatchRecord

- **Table:** `ManufacturingBatchRecord`
- **Definition:**

```prisma
model ManufacturingBatchRecord {
  id               String                   @id @default(cuid())
  tenantId         String
  batchNumber      String
  productId        String
  workOrderId      String?
  status           ManufacturingBatchStatus @default(IN_PROGRESS)
  startDate        DateTime?
  endDate          DateTime?
  yield            Float?
  theoreticalYield Float?
  actualYield      Float?
  operatorNotes    String?
  reviewedBy       String?
  reviewDate       DateTime?
  approvedBy       String?
  approvalDate     DateTime?
  deviations       Json?
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt

  @@index([tenantId])
  @@map("manufacturing_batch_records")

}
```

### BatchTraceability

- **Table:** `BatchTraceability`
- **Definition:**

```prisma
model BatchTraceability {
  id            String                @id @default(cuid())
  tenantId      String
  batchNumber   String
  productId     String
  direction     TraceabilityDirection
  parentBatchId String?
  childBatchId  String?
  materialName  String?
  quantity      Float?
  unit          String?
  stage         String?
  timestamp     DateTime?
  operator      String?
  notes         String?
  createdAt     DateTime              @default(now())

  @@index([tenantId])
  @@map("batch_traceabilities")

}
```

### RFQ

- **Table:** `RFQ`
- **Definition:**

```prisma
model RFQ {
  id                 String    @id @default(cuid())
  tenantId           String
  number             String
  title              String
  status             RFQStatus @default(DRAFT)
  supplierId         String?
  items              Json?
  dueDate            DateTime?
  submittedDate      DateTime?
  evaluationCriteria String?
  notes              String?
  createdBy          String?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt

  @@index([tenantId])
  @@map("rfqs")

}
```

### GoodsReceipt

- **Table:** `GoodsReceipt`
- **Definition:**

```prisma
model GoodsReceipt {
  id              String             @id @default(cuid())
  tenantId        String
  number          String
  purchaseOrderId String?
  status          GoodsReceiptStatus @default(PENDING)
  receivedDate    DateTime?
  receivedBy      String?
  items           Json?
  qualityCheck    Boolean            @default(false)
  notes           String?
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt

  @@index([tenantId])
  @@map("goods_receipts")

}
```

### DeliveryNote

- **Table:** `DeliveryNote`
- **Definition:**

```prisma
model DeliveryNote {
  id           String             @id @default(cuid())
  tenantId     String
  number       String
  salesOrderId String?
  status       DeliveryNoteStatus @default(DRAFT)
  dispatchDate DateTime?
  deliveryDate DateTime?
  carrier      String?
  trackingNumber String?
  items        Json?
  receivedBy   String?
  notes        String?
  createdAt    DateTime           @default(now())
  updatedAt    DateTime           @updatedAt

  @@index([tenantId])
  @@map("delivery_notes")

}
```

### Shipment

- **Table:** `Shipment`
- **Definition:**

```prisma
model Shipment {
  id               String         @id @default(cuid())
  tenantId         String
  number           String
  type             ShipmentType
  status           ShipmentStatus @default(PLANNED)
  origin           String?
  destination      String?
  carrier          String?
  trackingNumber   String?
  mode             ShipmentMode?
  estimatedArrival DateTime?
  actualArrival    DateTime?
  items            Json?
  cost             Float?
  currency         String?
  notes            String?
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt

  @@index([tenantId])
  @@map("shipments")

}
```

### FiscalPeriod

- **Table:** `FiscalPeriod`
- **Definition:**

```prisma
model FiscalPeriod {
  id         String             @id @default(cuid())
  tenantId   String
  name       String
  year       Int
  period     Int
  startDate  DateTime
  endDate    DateTime
  status     FiscalPeriodStatus @default(OPEN)
  closedBy   String?
  closedDate DateTime?
  createdAt  DateTime           @default(now())
  updatedAt  DateTime           @updatedAt

  @@index([tenantId])
  @@map("fiscal_periods")

}
```

### TaxConfiguration

- **Table:** `TaxConfiguration`
- **Definition:**

```prisma
model TaxConfiguration {
  id            String   @id @default(cuid())
  tenantId      String
  name          String
  code          String
  type          TaxType
  rate          Float
  country       String?
  region        String?
  effectiveFrom DateTime?
  effectiveTo   DateTime?
  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([tenantId])
  @@map("tax_configurations")

}
```

### Cheque

- **Table:** `Cheque`
- **Definition:**

```prisma
model Cheque {
  id            String       @id @default(cuid())
  tenantId      String
  number        String
  bankAccountId String?
  type          ChequeType
  status        ChequeStatus @default(PENDING)
  amount        Float
  currency      String?
  payee         String?
  payer         String?
  issueDate     DateTime?
  clearanceDate DateTime?
  notes         String?
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  @@index([tenantId])
  @@map("cheques")

}
```

### Deal

- **Table:** `Deal`
- **Definition:**

```prisma
model Deal {
  id                String    @id @default(cuid())
  tenantId          String
  title             String
  contactId         String?
  contact           Contact?  @relation(fields: [contactId], references: [id])
  accountId         String?
  account           Account?  @relation(fields: [accountId], references: [id])
  ownerId           String?
  owner             User?     @relation("DealOwner", fields: [ownerId], references: [id])
  stage             String    @default("LEAD") // LEAD, QUALIFIED, PROPOSAL, NEGOTIATION, CLOSED_WON, CLOSED_LOST
  amount            Float?
  currency          String    @default("USD")
  probability       Int?
  expectedCloseDate DateTime?
  actualCloseDate   DateTime?
  source            String?   // WEB, REFERRAL, COLD_CALL, TRADE_SHOW, PARTNER, INBOUND, OUTBOUND, OTHER
  priority          String    @default("MEDIUM") // LOW, MEDIUM, HIGH, CRITICAL
  description       String?
  notes             String?
  tags              String?   // JSON array
  lostReason        String?
  competitors       String?   // JSON array
  nextStep          String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  activities DealActivity[]

  @@index([tenantId])
  @@map("deals")

}
```

### DealActivity

- **Table:** `DealActivity`
- **Definition:**

```prisma
model DealActivity {
  id          String   @id @default(cuid())
  tenantId    String
  dealId      String
  deal        Deal     @relation(fields: [dealId], references: [id])
  type        String   // CALL, EMAIL, MEETING, TASK, NOTE
  subject     String
  description String?
  date        DateTime
  duration    Int?
  userId      String?
  status      String   @default("COMPLETED")
  createdAt   DateTime @default(now())

  @@index([tenantId])
  @@index([dealId])
  @@map("deal_activities")

}
```

### Budget

- **Table:** `Budget`
- **Definition:**

```prisma
model Budget {
  id           String       @id @default(cuid())
  tenantId     String
  name         String
  fiscalYear   Int
  departmentId String?
  department   Department?  @relation(fields: [departmentId], references: [id])
  costCenterId String?
  costCenter   CostCenter?  @relation(fields: [costCenterId], references: [id])
  status       String       @default("DRAFT") // DRAFT, SUBMITTED, APPROVED, REJECTED, ACTIVE, CLOSED
  totalAmount  Float
  currency     String       @default("USD")
  startDate    DateTime
  endDate      DateTime
  description  String?
  approvedBy   String?
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt

  lines BudgetLine[]

  @@index([tenantId])
  @@map("budgets")

}
```

### BudgetLine

- **Table:** `BudgetLine`
- **Definition:**

```prisma
model BudgetLine {
  id              String      @id @default(cuid())
  tenantId        String
  budgetId        String
  budget          Budget      @relation(fields: [budgetId], references: [id])
  accountId       String
  costCenterId    String?
  januaryAmount   Float       @default(0)
  februaryAmount  Float       @default(0)
  marchAmount     Float       @default(0)
  aprilAmount     Float       @default(0)
  mayAmount       Float       @default(0)
  juneAmount      Float       @default(0)
  julyAmount      Float       @default(0)
  augustAmount    Float       @default(0)
  septemberAmount Float       @default(0)
  octoberAmount   Float       @default(0)
  novemberAmount  Float       @default(0)
  decemberAmount  Float       @default(0)
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt

  @@index([tenantId])
  @@index([budgetId])
  @@map("budget_lines")

}
```

### CostCenter

- **Table:** `CostCenter`
- **Definition:**

```prisma
model CostCenter {
  id           String       @id @default(cuid())
  tenantId     String
  code         String
  name         String
  description  String?
  parentId     String?
  parent       CostCenter?  @relation("CostCenterHierarchy", fields: [parentId], references: [id])
  children     CostCenter[] @relation("CostCenterHierarchy")
  managerId    String?
  departmentId String?
  isActive     Boolean      @default(true)
  budgetAmount Float?
  currency     String       @default("USD")
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt

  budgets Budget[]

  @@index([tenantId])
  @@map("cost_centers")

}
```

### Position

- **Table:** `Position`
- **Definition:**

```prisma
model Position {
  id               String   @id @default(cuid())
  tenantId         String
  code             String
  title            String
  description      String?
  departmentId     String?
  department       Department? @relation(fields: [departmentId], references: [id])
  level            String?  // INTERN, JUNIOR, MID, SENIOR, LEAD, MANAGER, DIRECTOR, VP, C_LEVEL
  minSalary        Float?
  maxSalary        Float?
  currency         String   @default("USD")
  isActive         Boolean  @default(true)
  headCount        Int      @default(1)
  filledCount      Int      @default(0)
  requirements     String?
  responsibilities String?
  reportsTo        String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  employees Employee[]

  @@index([tenantId])
  @@map("positions")

}
```

